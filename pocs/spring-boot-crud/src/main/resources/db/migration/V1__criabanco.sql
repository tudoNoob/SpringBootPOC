create table pessoa(
	id BIGINT GENERATED BY DEFAULT AS IDENTITY,
	nome varchar(50),
	idade int

);

insert into pessoa (nome,idade) values('william', 12);

insert into pessoa (nome,idade)values('pedro', 12);

